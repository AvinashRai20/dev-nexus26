import { useEffect, useState } from 'react';
import { Link, useParams } from 'react-router-dom';
import api from '../services/api';
import Breadcrumbs from '../components/Breadcrumbs';
type Tool = { name: string; description: string; category: string; pricingType: string; websiteUrl: string; features?: string[]; useCases?: string[]; howToUse?: string; pros?: string[]; limitations?: string[]; isPremium?: boolean };
export default function AIToolDetail() { const { slug } = useParams(); const [tool, setTool] = useState<Tool | null>(null); useEffect(() => { if (slug) api.get(`/ai-tools/${slug}`).then((r) => setTool(r.data)).catch(console.error); }, [slug]); if (!tool) return <div className="mx-auto max-w-4xl px-4 py-20 text-center text-slate-500">Loading tool details…</div>; return <article className="mx-auto max-w-4xl px-4 py-12"><Breadcrumbs items={[{ label: 'AI Tools', to: '/ai-tools' }, { label: tool.name }]} /><Link to="/ai-tools" className="text-sm font-semibold text-blue-600">← Back to AI Tools</Link><div className="mt-8 rounded-3xl border border-slate-200 bg-white p-8 shadow-sm"><div className="flex flex-wrap items-center gap-3"><span className="rounded-full bg-blue-50 px-3 py-1 text-xs font-bold text-blue-700">{tool.category}</span><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold text-slate-600">{tool.pricingType}</span></div><h1 className="mt-5 text-4xl font-bold">{tool.name}</h1><p className="mt-4 text-lg leading-8 text-slate-600">{tool.description}</p><a href={tool.websiteUrl} target="_blank" rel="noreferrer" className="mt-6 inline-block rounded-lg bg-blue-600 px-5 py-3 font-semibold text-white">Try {tool.name}</a><div className="mt-10 grid gap-8 md:grid-cols-2"><Info title="Key features" items={tool.features} /><Info title="Best use cases" items={tool.useCases} /><Info title="Pros" items={tool.pros} /><Info title="Limitations" items={tool.limitations} /></div>{tool.howToUse && <div className="mt-8 border-t border-slate-100 pt-8"><h2 className="font-bold">How to use it</h2><p className="mt-2 whitespace-pre-line text-slate-600">{tool.howToUse}</p></div>}</div></article>; }
function Info({ title, items }: { title: string; items?: string[] }) { return <div><h2 className="font-bold">{title}</h2><ul className="mt-3 list-disc space-y-2 pl-5 text-sm text-slate-600">{(items?.length ? items : ['Details coming soon']).map((item) => <li key={item}>{item}</li>)}</ul></div>; }
