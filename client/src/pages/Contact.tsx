import { useState } from 'react';
import api from '../services/api';
export default function Contact() { const [form, setForm] = useState({ name: '', email: '', subject: '', message: '', category: 'General Feedback' }); const [sent, setSent] = useState(false); const submit = async (e: React.FormEvent) => { e.preventDefault(); await api.post('/feedback', form); setSent(true); setForm({ ...form, subject: '', message: '' }); }; return <section className="mx-auto max-w-2xl px-4 py-12"><h1 className="text-4xl font-bold">We’d love your feedback</h1><p className="mt-3 text-slate-500">Found a bug, have a course idea, or want to say hello? Send a note to the Dev.Nexus26 team.</p>{sent && <p className="mt-6 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-700">Thanks — your message has been sent.</p>}<form onSubmit={submit} className="mt-8 space-y-4 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">{(['name', 'email', 'subject'] as const).map((key) => <input key={key} required value={form[key]} onChange={(e) => setForm({ ...form, [key]: e.target.value })} placeholder={key[0].toUpperCase() + key.slice(1)} type={key === 'email' ? 'email' : 'text'} className="w-full rounded-lg border border-slate-300 px-4 py-3 outline-none focus:ring-2 focus:ring-blue-500" />)}<select value={form.category} onChange={(e) => setForm({ ...form, category: e.target.value })} className="w-full rounded-lg border border-slate-300 px-4 py-3"><option>General Feedback</option><option>Bug Report</option><option>Feature Request</option><option>Course Request</option><option>AI Tool Request</option><option>Other</option></select><textarea required rows={6} value={form.message} onChange={(e) => setForm({ ...form, message: e.target.value })} placeholder="Your message" className="w-full rounded-lg border border-slate-300 px-4 py-3 outline-none focus:ring-2 focus:ring-blue-500" /><button className="rounded-lg bg-blue-600 px-6 py-3 font-semibold text-white hover:bg-blue-700">Send message</button></form></section>; }
