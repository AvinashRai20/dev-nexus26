import { useEffect, useState } from 'react';
import api from '../services/api';
type Roadmap = { _id: string; title: string; description: string; steps: { title: string; description?: string }[] };
export default function Roadmaps() { const [maps, setMaps] = useState<Roadmap[]>([]); useEffect(() => { api.get('/roadmaps').then((r) => setMaps(r.data)).catch(console.error); }, []); return <section className="mx-auto max-w-7xl px-4 py-12"><p className="font-semibold text-violet-600">STEP-BY-STEP PATHS</p><h1 className="mt-2 text-4xl font-bold">Developer roadmaps</h1><div className="mt-10 space-y-8">{maps.length ? maps.map((map) => <article key={map._id} className="rounded-2xl border border-slate-200 bg-white p-7 shadow-sm"><h2 className="text-2xl font-bold">{map.title}</h2><p className="mt-2 text-slate-500">{map.description}</p><div className="mt-6 flex flex-wrap gap-3">{map.steps.map((step, i) => <div key={`${step.title}-${i}`} className="rounded-xl bg-slate-50 px-4 py-3"><span className="mr-2 text-xs font-bold text-blue-600">{String(i + 1).padStart(2, '0')}</span><span className="font-semibold">{step.title}</span></div>)}</div></article>) : <p className="text-slate-500">Roadmaps are being curated.</p>}</div></section>; }
